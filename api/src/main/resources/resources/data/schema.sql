CREATE TABLE person (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
    firstname VARCHAR(255) NOT NULL,
    lastname VARCHAR(255) NOT NULL,
    dob DATE NOT NULL NOT NULL ,
    age NUMERIC(3, 2) NOT NULL
    address VARCHAR(255)
);

